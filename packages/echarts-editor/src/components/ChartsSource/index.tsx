import { defineComponent } from 'vue'
import {} from 'element-plus'

export default defineComponent({
  setup(props, { emit }) {
    return () => (
      <div>
        ChartsSource
      </div>
    )
  }
})